<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalprojectapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/heartwithpusle"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/heartwithpusle"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProjectApp">
        <activity
            android:name=".login.ForgotPassword"
            android:exported="false"
            />
        <activity
            android:name=".Measurement_Results"
            android:exported="false" />
        <activity
            android:name=".login.RegisterActivity"
            android:exported="false" />

        <activity
            android:label="Pulse Oxime"
            android:name=".login.MainActivity"
            android:exported="true">
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>